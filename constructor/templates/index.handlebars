<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Birmingham.IO Technology Events Calendar</title>

    <!-- Bootstrap -->
    <link href="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css" rel="stylesheet">

    <!-- Birmingham.IO -->
    <link href="css/site.css" rel="stylesheet">

    <!-- HTML5 shim and Respond.js for IE8 support of HTML5 elements and media queries -->
    <!-- WARNING: Respond.js doesn't work if you view the page via file:// -->
    <!--[if lt IE 9]>
      <script src="https://oss.maxcdn.com/html5shiv/3.7.2/html5shiv.min.js"></script>
      <script src="https://oss.maxcdn.com/respond/1.4.2/respond.min.js"></script>
    <![endif]-->
  </head>
  <body>
    <div class='navbar-wrapper'>
        <div class='container'>
            <nav class="navbar navbar-default" role="navigation">
              <div class="container">
                <div class="navbar-header">
                  <button type="button" class="navbar-toggle collapsed" data-toggle="collapse" data-target="#navbar" aria-expanded="false" aria-controls="navbar">
                    <span class="sr-only">Toggle navigation</span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                    <span class="icon-bar"></span>
                  </button>
                  <a class="navbar-brand" href="#">Birmingham.IO Tech Calendar</a>
                </div>
                <div id="navbar" class="collapse navbar-collapse">
                  <ul class="nav navbar-nav">
                    <li class="active"><a href="#">Home</a></li>
                    <li><a href="#about">About</a></li>
                    <li><a href="#contact">Contact</a></li>
                  </ul>
                </div><!--/.nav-collapse -->
              </div>
            </nav>
        </div><!--/.container -->
    </div><!--/.navbar-wrapper -->

    <div class='container' id='calendar'>
        <h1>What's going on in tech in Birmingham</h1>

        <ul id="eventlist">
        {{#each upcoming}}
            <li class="upcoming">
                <a href="{{{this.htmlLink}}}" class="summary">{{{this.summary}}}</a>
                <time datetime="{{this.start.dateTime}}">{{{this.date_as_str}}}</time>
                <span class="desc">{{breaklines_linkify description}}</span>
                <a class="loc" href="https://www.google.com/maps?q={{{this.url_escaped_location}}}">{{this.location}}</a>
            </li>
        {{/each}}
        {{#each thisweek}}
            <li class="thisweek"><a href="{{{this.htmlLink}}}" class="summary">{{{this.summary}}}</a>
                <time datetime="{{this.start.dateTime}}">{{{this.date_as_str}}}</time>
                <a class="loc" href="https://www.google.com/maps?q={{{this.url_escaped_location}}}">{{this.location}}</a>
            </li>
        {{/each}}
            <li class="remaining">For more on these and {{remaining}} more events, see <a href="https://www.google.com/calendar/embed?showTitle=0&mode=AGENDA&ctz=Europe/London&src=movdt8poi0t3gfedfd80u1kcak@group.calendar.google.com&ctz=Europe/London">the full Google calendar</a>.</li>
        </ul>
    </div>

    <div class='container' id='contact'>
        <h2>How do I add my event to the Birmingham.IO calendar?</h2>
        <p class="well">The easiest way to become part of the Birmingham.IO calendar is to put your event on <a href="http://meetup.com">Meetup</a>. Be sure to tag it with <strong>technology</strong> and then it will be
        automatically included in the calendar with no other work required.</p>
        <p>If your event isn't on meetup, you can post a message in the 
        <a href='https://talk.birmingham.io/t/calendar-of-events-project-idea/'>Birmingham.IO discussion thread</a>
        and one of the calendar maintainers can add it for you, or if you're technical you can send a 
        <a href='https://github.com/BirminghamIO/calendar.birmingham.io/blob/master/constructor/explicitIcalUrls.json'>Github pull request</a> to the calendar's list.</p> 
    </div>

    <div class='container' id='about'>
        <h2>About</h2>
        <p>This is a calendar of all of the technology related meetups happening in Birmingham. The calendar
        was created and is maintained by the <a href='http://birmingham.io'>Birmingham.IO</a> community.</p>
    </div>

    <footer>
        <div class='container'>
            <p>Birmingham Technology Events Calendar &middot; <a href='http://birmingham.io'>Birmingham.IO</a></p>
        <div>
    </footer>

    <!-- jQuery (necessary for Bootstrap's JavaScript plugins) -->
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/1.11.1/jquery.min.js"></script>
    <!-- Include all compiled plugins (below), or include individual files as needed -->
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/js/bootstrap.min.js"></script>
    <script src="js/moment.js"></script>
    <script src="js/app.js"></script>
  </body>
</html>
